guard 'livereload' do
  watch(%r{\.(css|scss|js|html|php)})
end

guard 'sass',
  input: 'assets/style/sass',
  output: 'assets/style/compiled',
  style: :compressed,
  all_on_start: true,
  shallow: true